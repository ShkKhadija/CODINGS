#include<stdio.h>
int main() 
{
    int a, b, c;
   printf("Enter a value of a:");
    scanf("%d", &a); 
    
    printf ("Enter a value of b:");
    scanf ("%d", &b);
    c=a+b;
    
    printf ("Sum of given two numbers is:%d", c);
    
  return 0;
}
