#include <stdio.h>
#include <math.h>

int main()
{
    int a, b, c;
    float s, A;
    
    printf("Enter the values of a., b, c:\n");
    scanf("%d %d %d",&a, &b, &c);
    
    s = (a+b+c)/2;
    A = sqrt (s*(s-a)(s-b)(s-c));
    
    printf("Area of triangle is:%f", A);
    

    return 0;
}
